dist/
Source/ConnectMod/bin/
Source/ConnectMod/obj/
__pycache__/
.venv/
# uv.lock is NOT ignored: make test runs uv with --frozen and dependabot's
# "uv" ecosystem both require the lockfile to be committed.
coverage.cobertura.xml
cov-*.xml
coverage.svg
coverage-out/

# Scratch: every temporary, experimental, or bisect file lives here and is
# never committed. See the org REPOSITORY_STANDARDS.md.
.scratch/
.scratch_*
